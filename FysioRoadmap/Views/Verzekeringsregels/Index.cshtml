@model IEnumerable<FysioRoadmap.Models.Verzekeringsregel>

@{
    ViewBag.Title = "Verzekeringsregel";
}

<h2>Verzekeringsregel</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VerzekeringSet.Naamverzekering)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Minleeftijd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Maxleeftijd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BedragBehandeling)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Jaartotaal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VerzekeringSet.Naamverzekering)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Minleeftijd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Maxleeftijd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BedragBehandeling)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Jaartotaal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
